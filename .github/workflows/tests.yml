name: Tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: audiojrn
          POSTGRES_USER: audiojrn
          POSTGRES_PASSWORD: audiojrn_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U audiojrn -d audiojrn"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install test dependencies
        run: python -m pip install -r requirements-test.txt
      - name: Apply database migrations
        env:
          DATABASE_URL: postgresql+psycopg://audiojrn:audiojrn_ci@localhost:5432/audiojrn
          APP_TIMEZONE: UTC
        run: alembic upgrade head
      - name: Run tests without external services
        env:
          DATABASE_URL: postgresql+psycopg://audiojrn:audiojrn_ci@localhost:5432/audiojrn
          APP_TIMEZONE: UTC
        run: python -m pytest -q
      - name: Run evaluation infrastructure tests
        run: python -m pytest -q tests/test_evaluation.py
